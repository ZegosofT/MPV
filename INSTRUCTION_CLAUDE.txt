Can you modify this config to just order like this :

Look the space between the key bind on the left and the action and always keep it so it's a list that always start at the same point on the left and right

On top : put the title in the middle as 
###########
#  Mouse  #
###########

and put all the mouse bind that exist in the config files.

Under that put this and add a title for it
F1 to F12  in order and same instruction as the alphabetic keys for ctrl alt and shift 
Reminder : always start the key on the left and the action on the right at the same point for make it easier to read

Under it with another title :
All the key "&é"'(-è_çà)=1234567890°+~#{[|`\^@]}"
I have a french keyboard this are the keys above azerty. Just write everything in the order I sent you and and same instruction as the alphabetic keys for ctrl alt and shift


Under it with another title :
Write all the keys in alphabetic from a to z, for the commands that exist already put them, if not then put nothing and as argument
All the combination with ALT and SHIFT and CTRL, put it at the same place as the key and same for the capital, for example I write you some so you understand :

a                                   script-binding uosc/audio
A                                   cycle audio
CTRL+SHIFT+A                        cycle-values audio-spdif "ac3,dts,eac3,truehd,dtshd" "no" ; show-text "Audio: ${?audio-spdif==no:PCM (Upmix Active)}${!audio-spdif==no:Bitstream (Passthrough)}"
CTRL+a                              script-binding auto_audio_device/toggle-switching

b                                   cycle deband
CTRL+b                              script-message toggle-anime-fidelity

d                                   cycle deinterlace
D                                   script-binding open-anime-menu
...

Under put
###########
# Nav Keys #
###########

Same principle as the keys from A to Z with the order being :
LEFT
RIGHT
UP
DOWN
TAB
-
+
"and continue if some keys are missing you choose the order for the rest"
